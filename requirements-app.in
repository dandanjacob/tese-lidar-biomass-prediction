# Dependências do APP Streamlit (deploy).
# O Streamlit Community Cloud instala a partir do requirements.txt — que é compilado
# DESTE arquivo. Mantido enxuto de propósito: sem torch/jupyterlab/scikit-learn/etc.
# (pesam e quebram o build no Cloud). O lock completo de treino é requirements-dev.txt.
streamlit>=1.40
folium>=0.17
plotly>=5.24
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
geopandas>=0.14
shapely>=2.0
laspy[lazrs]>=2.5
